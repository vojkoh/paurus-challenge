import { CreateEnrollmentDto } from './create-enrollment-dto';

describe('CreateEnrollmentDto', () => {
  it('should create an instance', () => {
    expect(new CreateEnrollmentDto()).toBeTruthy();
  });
});
